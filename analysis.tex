\subsection{Data Collection}

Data was collected from the website timeanddate.com, which tracks the the amount of daylight each day all over the country.
Sunrise times were logged in a .csv file in columns for each month.
They were logged as strings in the format of hh:mm, for hours and minutes.

Before analysis could begin, data found through~\parencite{timeanddate.com} had to be processed before finding the averages of each month could be found.
All values of interest represented as strings had to be converted into intergers.
This was done by using techniques from ~\cite{Scopatz2015} to separate each of the strings by the colon use in the values collected for times.
From there the minutes had to be coverted to a decimal of the hour, and recombined with the hour the minute are in.
With that the data was now able to be analyzed to find the average sunrise time of each month.

\subsection{Data Aanlysis}

With sunrises times from all 365 days in 2024 available the averages of each month were taken \ref{fig:averages}.
This was done using the general formula for calculate mean\ref{eqn:average}.

\begin{equation}
\text{Average} = \frac{\text{Sum of sunrise times in the month}}{\text{Numer of days in the month}}
\label{eqn:average}
\end{equation}
Once this was done for each month the twelve calculated averages were plotted on a graph with sunrise time over months \ref{fig:sunrise}.

\begin{figure}[h]
	\centering
	\includegraphics[width=0.7\textwidth]{data_table_from_csv.png}
	\caption{List of the average sunrises for each month in 2024}
	\label{fig:averages}
\end{figure}

\begin{figure}[h]
	\centering
	\includegraphics[width=0.7\textwidth]{Figure_1.png}
	\caption{Graph of the sunrise averages for each month in the year 2024}
	\label{fig:sunrise}
\end{figure}
